x_speed = 0;
y_speed = 0;

move_speed = 2;
custom_gravity = .25;

// jump
jump_speed = -2.5;

jump_max = 2; // max sequential jumps
jump_count = 0; // tracks the current jump

jump_height = 25; // the number of frames that a player jumps
jump_timer = 0; // tracks the current jump frame
